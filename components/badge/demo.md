# badge 按钮

## 基本用法

```jsx
import { Badge, Button } from 'react-vant';

ReactDOM.render(
  <>
    <Badge content={12}>
      <Button>default</Button>
    </Badge>
    <Button theme="primary">primary</Button>
  </>,
  mountNode,
);
```
